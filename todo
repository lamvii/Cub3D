check range of colors ##done
fixed the return of atoi if tmp dont have 3 cases
check that texteur have .xpm
tqadi tkatbi mn hnaya

free tmp 'split alocate memory'
check tab_length == 3 'you can use map_length()' else ==> eror
foreach tmp[i] check tmp[i][j] is digit else ==> error
then use atoi(tmp[i]) == > check range
